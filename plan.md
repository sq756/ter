# SSH 可视化管理平台 (代号: Ter) 开发计划

## 0. 项目核心理念
**“安全、隔离、无感”**：在服务器不暴露任何公网端口的前提下，通过加密的 SSH 隧道实现全方位的可视化管理、实时监控与常驻进程托管。

---

## 1. 架构概览与技术栈

### 1.1 系统架构
```text
[ 桌面端 (Tauri) ] <--- SSH 隧道 (Port Forwarding) ---> [ 服务器 (Linux/Unix) ]
      |                                                   |
      +-- 前端 (Vue/React): UI 渲染 & xterm.js             +-- Go Agent: 系统监控
      +-- 后端 (Rust): SSH 连接 & 隧道管理                  +-- 业务进程: 托管运行
      +-- 数据库 (SQLite): 配置持久化
```

### 1.2 技术栈选型
| 模块 | 技术栈 | 核心职责 |
| :--- | :--- | :--- |
| **桌面端 UI (前端)** | Vue 3 / React + TypeScript + xterm.js | 终端渲染、可视化图表 (ECharts)、多服务器面板管理。 |
| **桌面端 Core (Rust)** | Tauri + russh (或 ssh2) + tokio + SQLx | 管理 SSH 隧道、SFTP 传输、本地 API 转发、加密存储服务器配置。 |
| **服务端守护进程 (Agent)** | Go (Static Compilation) | 系统指标收集 (gopsutil)、进程树管理、Local API (127.0.0.1)。 |
| **通信协议** | SSH Tunnel + JSON RPC over HTTP | 确保数据在物理层通过 SSH 加密，逻辑层通过标准 JSON 交互。 |

---

## 2. 详细开发阶段

### 第一阶段：基础框架与终端互通 (Terminal & SSH)
*   **Tauri 项目初始化**: 构建支持多窗口和系统托盘的基础架子。
*   **Rust SSH 引擎**:
    *   集成 `russh` 异步库，支持密码和 SSH-Key 认证。
    *   实现 **PTY (伪终端)** 通道，支持终端大小动态自适应 (Window Resize)。
*   **前端终端集成**:
    *   使用 `xterm.js` 及其 WebGL 加速插件。
    *   通过 Tauri `emit/listen` 事件流，打通前端输入到 Rust SSH 通道的低延迟双向绑定。
*   **目标**: 能够稳定连接远程服务器，并在自定义 UI 中流畅使用命令行。

### 第二阶段：自研守护进程 (Go Agent)
*   **轻量化设计**: Agent 需保持在 10MB 以内，内存占用 < 20MB。
*   **指标采集**:
    *   CPU/内存/磁盘/网络实时速率。
    *   **进程快照**: 获取当前占用资源排名前 10 的进程。
*   **进程控制 API**:
    *   提供 `/proc/kill`, `/proc/restart` 等接口。
    *   **常驻托管**: 模拟 `screen/tmux` 功能，接管进程的 `stdout/stderr` 并持久化到本地文件。
*   **安全加固**: Agent 仅监听 `127.0.0.1`，并引入简易 Token 校验（由桌面端随机生成）。

### 第三阶段：自动化部署与隧道穿透 (Tunneling)
*   **静默安装 (The "Zero-Config" Experience)**:
    *   SSH 连接后，Rust 自动检测 `~/.ter/agent` 版本。
    *   版本不匹配时，通过 **SFTP** 自动静默下发对应的二进制文件。
    *   执行自启动脚本，确保 Agent 后台运行。
*   **本地端口转发 (L-Forwarding)**:
    *   在 Rust 层建立隧道：将本地随机端口 (如 `localhost:54321`) 映射到服务器 `127.0.0.1:34567` (Agent 端口)。
    *   前端所有可视化数据请求均指向 `localhost:54321`。
*   **目标**: 用户只需输入 SSH 账号，其余 Agent 部署与隧道打通全部自动完成。

### 第四阶段：可视化面板与任务持久化 (Dashboard)
*   **实时仪表盘**:
    *   资源利用率动态曲线图。
    *   可交互的进程管理器：右键结束进程、查看进程环境变量。
*   **断连恢复 (Persistence)**:
    *   即使桌面端关闭，Agent 仍在服务器运行。
    *   重新连接后，UI 自动拉取 Agent 托管的任务列表和历史日志。
*   **文件管理 (可视化 SFTP)**:
    *   提供类似侧边栏的树形文件浏览器，支持拖拽上传/下载。

### 第五阶段：AI 助手与增强功能 (Advanced)
*   **AI Sidekick**:
    *   在侧边栏嵌入 WebView 加载 AI 助手。
    *   **一键诊断**: 终端报错时，自动抓取最后 N 行日志，发送给 AI 进行修复建议分析。
*   **多服务器管理**:
    *   支持“工作区”概念，可同时监控多个服务器的状态。
*   **安全性增强**:
    *   主密码加密本地 SQLite 数据库。
    *   操作审计日志：记录所有通过 UI 执行的敏感命令。

---

## 3. 查漏补缺与细节细化

### 3.1 核心需求补全
1.  **Jump Host (跳板机) 支持**: 许多内网服务器需要通过跳板机访问，架构需支持多级 SSH 隧道。
2.  **网络自动重连**: 处理 WiFi 切换或休眠后的 SSH 心跳检测与自动重連逻辑。
3.  **Agent 自动清理**: 提供“卸载”选项，清理远程服务器上的 Agent 文件和常驻任务。
4.  **性能监控历史**: Agent 本地暂存过去 1 小时的基础指标，以便用户上线时能看到近期趋势。

### 3.2 架构优化细节
*   **通信层优化**: 使用长连接 WebSocket 代替频繁的 HTTP 轮询，减少 SSH 隧道内的 RTT 延迟。
*   **并发控制**: Rust 后端采用任务池管理多个 SSH 通道，防止单个高耗时任务（如大数据下载）阻塞监控数据流。
*   **本地存储**: 使用 SQLite 存储服务器标签、连接记录、私钥路径等，并对私钥路径进行脱敏处理。

### 3.3 非功能性需求
*   **跨平台**: 桌面端支持 macOS (Intel/M-series), Windows, Linux；Agent 需支持主流 Linux 发行版。
*   **资源敏感度**: 在低功耗服务器上，Agent 必须具备自我限速功能，避免干扰业务进程。
